-r requirements.txt
# MCP stdio: tools/mcp_latex_server.py. Use official `mcp` (includes FastMCP). Do not pip install the separate `fastmcp` PyPI package (Starlette conflicts with FastAPI).
mcp>=1.0,<2
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
